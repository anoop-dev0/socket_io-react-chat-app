{
  "name": "server-app",
  "version": "1.0.0",
  "description": "server for a chat application.",
  "main": "index.js",
  "scripts": {
    "devStart": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anoop Kumar",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
